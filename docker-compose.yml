services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: hybrid_rag_qdrant
    ports:
      - "6333:6333"   # REST / gRPC API
      - "6334:6334"   # gRPC
    volumes:
      - ./qdrant_storage:/qdrant/storage   # persisted index (gitignored)
    restart: unless-stopped
